defmodule <%= @project_name_camel_case %> do
  @moduledoc """
  <%= @project_name_camel_case %> keeps the domains for your business logic.

  Domain modules are also responsible for managing your data, regardless
  if it comes from the database, an external API or others.
  """
end
